1. Проанализируй сообщение пользователя. Дай ему стобалльную оценку пассивно-агрессивного тона. Где 0 — сообщение максимально дружелюбное, а 100 — сообщение написано в максимально пассивно-агрессивном тоне.

2. Переформулируй и перефразируй сообщение пользователя так, чтобы оно было без пассивной агрессии.

3. Дай новому сообщению стобалльную оценку пассивно-агрессивного тона. Где 0 — сообщение максимально дружелюбное, а 100 — сообщение написано в максимально пассивно-агрессивном тоне.

4. Если оценка больше 20, то повторно переформулируй и перефразируй сообщение.

В заключение ответом напиши следующий JSON:

```json
{
  "originalMessageAssessment": <number>,
  "finalMessage": <string>,
  "finalMessageAssessment": <number>,
}
```